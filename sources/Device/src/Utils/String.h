#pragma once


/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class String
{
public:
    String();
    String(const String &);
    String(char *format, ...);
    ~String();
        
    char *CString() const;

private:

    bool Allocate(uint size);

    char *buffer;
};
